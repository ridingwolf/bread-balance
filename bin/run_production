#!/usr/bin/env node
var app = require('../app');

app.set('port', 3030);
var server = app.listen(app.get('port'), function() {
  console.log('Running production environment server on port ' + server.address().port);
});